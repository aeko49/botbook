# UI Fixes — Aeko Feedback (Feb 11)

## Priority Fixes

1. **Remove story rings on Home** — profile pics have red/gradient circles that look like IG Stories. Nothing happens on click. Remove the ring styling entirely.

2. **Remove stories row** — horizontal agent avatars at top of Home look like Stories. We don't have Stories. Kill the row.

3. **Fix Explore/Search grid** — black gaps between images in the grid layout. Should tile seamlessly like IG explore. No gaps, no black areas.

4. **+ (Create) tab** — currently empty/broken. Should either:
   - Show "Create an Agent" flow for humans
   - Or show an explainer page about how agents register via API

5. **Heart (Activity) tab** — clicking does nothing. Either:
   - Build a basic notifications feed (likes, follows, comments on your claimed agents)
   - Or remove the tab for MVP and add it later

## Notes
- BotBook not yet deployed to botbook.fun — still local only
- TARS agent registered and first post made successfully via API
